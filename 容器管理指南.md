# 🚨 發現多個容器運行

## 當前狀態
發現 **2 個容器**同時運行：
1. `0c18f6271b90` (1 分鐘前) - **新的**
2. `78037dba3476` (40 分鐘前) - **舊的**

⚠️ **問題**: 兩個容器會競爭資源和檔案，可能造成衝突！

---

## 🔧 立即處理

### 方法 1: 停止舊容器（推薦）

```powershell
# 停止舊容器（40 分鐘前的）
docker stop 78037dba3476

# 查看新容器的日誌
docker logs -f 0c18f6271b90
```

### 方法 2: 停止所有容器，重新開始

```powershell
# 停止所有下載容器
docker stop 0c18f6271b90 78037dba3476

# 重新啟動（只運行一個）
cd D:\auto_downloader
docker run --rm -v "${pwd}\data:/app/data" `
    -e RUNS_FILE=runs_to_fix.txt `
    -e MAX_WORKERS=8 `
    -e USE_ARIA2=yes `
    auto_downloader
```

---

## 📊 監控單個容器

### 查看日誌（新容器）
```powershell
docker logs -f 0c18f6271b90
```

### 檢查進度
```powershell
cd D:\auto_downloader
Get-Content data\download_progress.json | ConvertFrom-Json | Select -ExpandProperty runs | ForEach-Object { $_.PSObject.Properties | Where-Object {$_.Value.status -eq "completed"} | Measure-Object } | Select -ExpandProperty Count
```

---

## ⚠️ 為什麼要停止舊容器？

1. **資源競爭**: 兩個容器同時下載會慢一半
2. **檔案衝突**: 可能同時寫入相同檔案造成損壞
3. **NAS 連接**: 多個連接可能被限制

---

## ✅ 建議操作

**立即執行:**
```powershell
# 1. 停止舊容器
docker stop 78037dba3476

# 2. 監控新容器
docker logs -f 0c18f6271b90
```

這樣新容器就能全速運行了！🚀
